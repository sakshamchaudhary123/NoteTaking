<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Notes</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            class="rounded-md outline-none w-full bg-zinc-800 block px-5 py-3"
            type="text"
            placeholder="Write title over here"
            name="title"
          />
          <textarea
            class="rounded-md outline-none w-full bg-zinc-800 block px-5 py-3 resize-none mt-2"
            placeholder="write your task details over here...."
            name="details"
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks flex gap-3 flex-wrap p-10">
        <% if(files.length > 0) { %>
            <% files.forEach((val) => { %>
                <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-md">
                    <h1 class="text-white text-2xl tracking-tighter">
                      <%= val %>
                    </h1>
                    <div class="flex w-full justify-between items-center mt-5">
                    <a href="/file/<%= val %>" class="text-blue-500 inline-block">Read more</a>
                    <a class="text-zinc-600" href="/edit/<%= val%>">Edit filename</a>
                    </div>
                  </div>
            <% }) %>
        <% } else { %>
        <h3 class="text-zinc-700">No task yet.</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
